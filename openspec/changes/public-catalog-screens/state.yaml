change: public-catalog-screens
schema: spec-driven-state/v1

session_preflight:
  pace: interactive
  artifact_store: hybrid
  delivery_strategy: ask-on-risk
  chain_strategy: stacked-to-main
  review_budget_lines: 400

dag:
  proposal: complete
  specs: complete
  design: complete
  tasks: complete
  apply: complete
  verify: complete
  archive: pending

verify_result:
  verdict: pass_with_warnings
  independently_verified:
    - "npm test: 18 files / 120 tests, npm run lint clean, npm run build clean (own run)"
    - "git status clean; frontend/src/app/page.tsx confirmed genuinely deleted"
    - "live checks against running dev server + real seed: / , /catalog, /product/fundas-iphone-15 -> 200; /product/does-not-exist -> 404; /api/catalog variants (no params, ?q=funda, ?page=0, ?model=NoSuchModel) all returned correct status/shape"
    - "curled real HTML, grepped for cost*/quantity* with a positive-control grep (priceFrom) to prove the pipeline wasn't a no-op -- zero sensitive-field matches"
    - "read variant-picker.tsx in full: zero disabled= attributes anywhere"
    - "read queries.ts/route.ts: only .from() call site is CatalogRelation-typed (base table = compile error); zero select(\"*\") or service_role/SERVICE_ROLE anywhere in frontend/src"
    - "hand-traced runtime-caching.ts regexes against real routes -- all match; git log --follow shows exactly ONE commit ever on that file (the original scaffold), independent confirmation of byte-identity beyond the pinned-SHA256 test"
  issues:
    - severity: WARNING
      status: resolved
      description: >
        Engram-stored spec artifact (sdd/public-catalog-screens/spec) had stale
        "visible but disabled" wording for the out-of-stock requirement, contradicting
        the correct on-disk specs/public-catalog-ui/spec.md (fixed earlier, before
        sdd-tasks). Orchestrator's on-disk fix never propagated to the Engram mirror.
      resolution: "Re-saved/upserted the Engram topic_key sdd/public-catalog-screens/spec with the correct current on-disk text (mem_save id 294)."
  critical_issues: none

phases_completed:
  - explore
  - propose
  - spec
  - design
  - tasks
  - apply

review_workload_decision:
  budget_risk: high
  estimated_changed_lines: "~950-1050 (own file-by-file estimate, above design's ~750-960)"
  chained_prs: true
  suggested_split: "PR1 foundation (lib/catalog, lib/supabase, next.config.ts, env) -> PR2 pages/UI -> PR3 search API"
  chain_strategy: stacked-to-main
  delivery_strategy: ask-on-risk
  decision_needed_before_apply: true
  user_confirmed: "3 chained PRs, stacked-to-main"

pr_progress:
  pr1_foundation:
    status: complete
    tasks: "1.1-1.17 (17/17)"
    tests: "58/58 passing (8 test files) -- independently re-run by orchestrator"
    actual_changed_lines: 855
    budget_exception: >
      855 vs 400-line budget (~2x), driven by 58 Strict-TDD unit tests across 8 pure
      modules. User accepted as size:exception after ask-on-risk stop (declined
      splitting this cohesive, tightly-coupled foundation layer further).
    verified_by_orchestrator:
      - "column allowlist constants (CATALOG_PRODUCT_COLUMNS/CATALOG_VARIANT_COLUMNS/CATALOG_IMAGE_COLUMNS) spot-checked against the real view DDL in 20260810000458_public_catalog_rls.sql -- exact match, no sensitive columns"
      - "frontend/.env.local confirmed gitignored (git status shows only .env.example as tracked); no service_role key anywhere in frontend"
    sdd_attempt_ledger: "settled (state: complete, changed_lines: 1117 raw diff; reset once from 800 to 2000 max-changed-lines budget, same miscalibration pattern as initial-scaffolding PR1)"
    merged_to_main: fb1b649
  pr2_pages_ui:
    status: complete
    tasks: "2.1-2.21 (21/21)"
    tests: "100/100 passing (15 test files) -- independently re-run by orchestrator"
    actual_changed_lines: 1650
    budget_exception: "1650 vs 400-line budget, same Strict-TDD test-density pattern as PR1; accepted after ask-on-risk stop"
    verified_by_orchestrator:
      - "grepped variant-picker.tsx source: zero `disabled=` attributes, confirms out-of-stock swatches are real radio buttons, not disabled controls"
      - "confirmed frontend/src/app/page.tsx (scaffold) actually deleted via git status"
      - "live browser verification against a freshly-reset local Supabase (found and cleaned a stale 'RLS Check Phone' fixture row left over from supabase-schema's verify phase first): listing renders both real seeded products with correct 'Desde $12.990' / '$14.990' pricing; product detail page's Transparente (out-of-stock) swatch is a real ARIA radio, clicking it swaps price to $12.990 and shows a 'Sin stock' indicator with no page navigation and no console errors"
    docker_note: "Docker Desktop was not running when sdd-apply's own agent tried to manually verify (used graceful error-state fallback only) -- user started Docker Desktop themselves, orchestrator then ran supabase start + db reset + real browser verification before merge"
    sdd_attempt_ledger: "settled (state: complete, changed_lines within the 2500 max-changed-lines budget acquired upfront -- no reset needed this time, applied the PR1 lesson)"
    merged_to_main: 59f75cd
  pr3_search_api:
    status: complete
    tasks: "3.1-3.10 + 4.1-4.3 (13/13, Phase 3+4 combined -- final slice)"
    tests: "120/120 passing (18 test files) -- independently re-run by orchestrator"
    actual_changed_lines: 956
    budget_exception: "956 vs 400-line budget, same Strict-TDD test-density pattern as PR1/PR2; accepted after ask-on-risk stop"
    orchestrator_fix: >
      Found a pre-existing lint error inherited from PR2 (native <a> instead of next/link's
      <Link> in catalog-empty-state.tsx, @next/next/no-html-link-for-pages) via npm run lint
      -- not caught by the pinned Vitest test command, so Strict TDD never saw it. Fixed
      directly (2 lines, mechanical) since it was trivial and left dangling between PRs
      otherwise; re-confirmed lint clean and 120/120 tests still passing after the fix.
    verified_by_orchestrator:
      - "live browser: typed 'Galaxy' in search box, pressed Enter -- URL updated to ?q=Galaxy without reload, listing correctly filtered to only Funda Galaxy S24"
      - "live browser: searched a nonsense term -- distinct 'No encontramos productos para tu busqueda' no-results state rendered (different from PR2's empty-catalog state), with a working 'Limpiar filtros' link"
      - "raw curl of GET /api/catalog?q=Galaxy: response JSON contains id/slug/name/priceFrom/hasPriceRange/inStock(boolean)/colors/heroImageUrl/heroImageAlt/page/limit/total/totalPages/filters/applied -- confirmed NO cost field, NO exact stock quantity anywhere"
      - "console checked clean, no errors during any interaction"
    sdd_attempt_ledger: "settled (state: complete, changed_lines within the 2000 max-changed-lines budget acquired upfront -- no reset needed)"
    merged_to_main: 2c0db67

product_decisions:
  scope: "Full — listing + detail + color picker + search/filters/pagination (user overrode Medium recommendation, anticipates catalog growth)"
  caching_model: "classic ISR (export const revalidate = N), not Next 16 Cache Components — lower risk on new feature"
  data_access: "direct Supabase reads from Server Components via @supabase/ssr, anon key, catalog_* views only — carried forward from supabase-schema, not re-litigated"
  landing_route: "/ IS the listing (accepted default assumption)"
  freshness: "revalidate = 300 (5 min)"
  listing_price_display: "\"from {min}\" when variants differ in price"
  out_of_stock_display: >
    visible AND clickable with a "Sin stock" badge (refined during sdd-design: a literally
    disabled swatch would have made that color's image/price unreachable, contradicting
    the original rationale that availability is a real buying signal -- user confirmed
    the clickable variant). NOTE: specs/public-catalog-ui/spec.md was written by sdd-spec
    in parallel with sdd-design and initially retained the stale "disabled" wording from
    the proposal's original assumption -- orchestrator caught and fixed the drift after
    sdd-tasks flagged it as a Key Learning, before tasks/apply could act on the wrong spec.
  filter_source: "derived from catalog data, not a curated fixed list (accepted default assumption)"

design_findings_verified_by_orchestrator:
  - "catalog_products view has no `model` column (confirmed reading the migration SQL directly) -- phone-model filter must query catalog_variants.phone_model instead"
  - "supabase/seed.sql has zero hero images (all product_images inserts use non-null variant_id) -- confirmed reading seed.sql directly. Hero resolution falls back to default variant's first image / placeholder"
  - "frontend/src/app/page.tsx (scaffold default) collides with new (public)/page.tsx at route '/' -- design's file-change table marks the scaffold page for deletion"

known_risks:
  - "400-line review budget risk flagged High in proposal — pre-suggested 3 stacked slices (client+env+config / listing+detail+picker / route handler+search). sdd-tasks owns the real forecast."
  - "@supabase/ssr vs Next 16 async cookies() compatibility postdates training data — re-verify at install time"
  - "Empty catalog on first real (non-Docker) deploy is expected — empty-state UI is in-scope success criterion"

next_recommended: sdd-archive (WARNING was Engram-mirror drift only, resolved via topic_key upsert -- no code change needed)

notes: |
  Builds on archived supabase-schema (catalog_* views, RLS, product-photos bucket
  all live and verified in local Docker). frontend/src/app/ is still unmodified
  create-next-app scaffold going into this change — no route groups exist yet.
  Backend FastAPI explicitly untouched.
